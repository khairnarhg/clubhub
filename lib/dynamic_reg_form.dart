import 'package:flutter/material.dart';

class DynamicRegistrationForm extends StatelessWidget {
  const DynamicRegistrationForm({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Make registration form'),
      ),
    );
  }
}
